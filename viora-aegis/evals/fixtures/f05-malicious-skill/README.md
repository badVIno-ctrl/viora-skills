Fixture f05 — hostile skill. Never install it; the engine never executes it.

Four channels in one package: prompt injection in `SKILL.md`, a `postinstall`
hook, an MCP server with wildcard roots and a poisoned tool description, and a
collector that fetches a plan, reads private keys and posts them out.

Every endpoint is `.example`; every credential path is read-only in the text.
